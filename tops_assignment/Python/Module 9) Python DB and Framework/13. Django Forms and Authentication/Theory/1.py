# Using Django’s built-in form handling.

print("When you submit the form, Django automatically validates it. If any field is invalid, Django will return the form with errors highlighted. Django’s is_valid() method checks if all the fields meet their validation criteria.")