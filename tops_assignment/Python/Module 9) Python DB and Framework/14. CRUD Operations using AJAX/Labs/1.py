# Implement AJAX in a Django project for performing CRUD operations.

print("""

AJAX Works in Django
  - User triggers an event (Like : click button, submit a form, onchange event).

 - JavaScript (or jQuery) sends an AJAX request to the Django server.

 - Django processes the request in a view and returns a response 
   (JSON, HTML, etc.).

""")