# How to serve static files (like CSS, JavaScript) in Django.

print("To serve static files in Django, set STATIC_URL and STATICFILES_DIRS in settings.py, which will serve and store the CSS, JavaScript, and images.")