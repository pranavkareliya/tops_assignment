# Linking external or internal JavaScript files in Django.

print("Static files, like css, js, and images, goes in the static folder. If you do not have one, create it in the same location as you created the templates folder.")