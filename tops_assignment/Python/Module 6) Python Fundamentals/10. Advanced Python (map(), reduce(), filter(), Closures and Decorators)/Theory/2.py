# Using map(), reduce(), and filter() functions for processing data

# map
print("The map () function returns a map object(which is an iterator) of the results after applying the given function to each item of a given iterable")

# filter
print("The filter() method filters the given sequence with the help of a function that tests each element in the sequence to be true or not. ")

# reduce
print("The reduce function is used to apply a particular function passed in its argument to all of the list elements mentioned in the sequence passed along.")