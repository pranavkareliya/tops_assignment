/* 4. Find area of square formula :- area = a * a */
#include <stdio.h>

int main() {
    
    float a,area;
    
    printf("Enter the value of length : ");
    scanf("%f",&a);
    
    area=a*a;
    printf("The area of square is %f",area);

    return 0;
}
