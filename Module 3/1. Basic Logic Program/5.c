/* 5. Find area of cube formula :-  area = 6 * a *a */
#include <stdio.h>

int main() {
    
    float a,area;
    
    printf("Enter the value of length : ");
    scanf("%f",&a);
    
    area=6*a*a;
    printf("The area of square is %f",area);

    return 0;
}